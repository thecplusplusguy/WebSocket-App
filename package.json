{
  "name": "websocket-app",
  "version": "1.0.0",
  "description": "WebSocket server that returns random sensor/metric data",
  "main": "dist/src/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test dist/test/",
    "test:watch": "tsc --watch & node --test --watch dist/test/",
    "start": "npm run build && node dist/src/index.js",
    "dev": "tsc --watch"
  },
  "keywords": ["websocket", "sensor", "metrics"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/ws": "^8.5.10",
    "typescript": "^5.3.3"
  }
}